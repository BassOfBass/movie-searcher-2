import { useEffect } from "react";
import { useState } from "react";

export function TMDBList() {
  const [isLoading, changeIsLoading] = useState(false);
  const [error, setError] = useState(null);
  const [list, ssetList] = useState({});

  useEffect(() => ());
}