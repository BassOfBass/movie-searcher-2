import styles from "./pagination.module.scss";

/**
 * @param {object} props
 * @param {{}} props.stats
 * @returns 
 */
export function Pagination({ stats, callback }) {
  return (
    <div>
    </div>
  );
}