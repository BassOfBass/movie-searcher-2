/**
 * TODO: finish it
 * @param {*} name 
 * @param {*} id 
 * @returns 
 */
export function LanguageSelect(name = "iso_639_1", id = "list-lang-639") {
  return (
    <select name={name} id={id}></select>
  );
}